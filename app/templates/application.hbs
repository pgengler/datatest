{{page-title "Datatest"}}

{{outlet}}
<h1>Post {{this.model.id}}</h1>
<h5>Comments</h5>
<ul>
{{!-- {{#each @model.authorsTracked as |author|}} --}}
{{#each @model.authors as |author|}}
  <li>{{author.name}}</li>
{{/each}}
</ul>
